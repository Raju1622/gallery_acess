name: gallery_acess
description: A Flutter project with Google Sign-In functionality
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_sign_in: ^6.2.1
  firebase_auth: ^4.15.0
  firebase_core: ^2.24.2
  firebase_storage: ^11.6.5
  permission_handler: ^11.3.1
  photo_manager: ^3.5.0
  razorpay_flutter: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
